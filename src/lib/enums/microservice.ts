export enum Microservice {
    jobManager = 'job-manager',
    configManager = 'config-manager',
    sync = 'sync',
    worker = 'worker',
    status = 'status',
    backup = 'backup',
    all = 'all'
}
