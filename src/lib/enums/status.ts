export enum JobStatus {
    error = 'error',
    finished = 'finished',
    pending = 'pending',
    started = 'started'
}

export enum HintStatus {
    error = 'error',
    off = 'off',
    pass = 'pass',
    pending = 'pending',
    warning = 'warning'
}
