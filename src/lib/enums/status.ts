export enum JobStatus {
    error = 'error',
    finished = 'finished',
    pending = 'pending',
    started = 'started'
}

export enum RuleStatus {
    error = 'error',
    pass = 'pass',
    pending = 'pending',
    warning = 'warning'
}
